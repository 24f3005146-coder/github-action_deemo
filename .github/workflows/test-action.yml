name: Test Workflow with Email

on:
  workflow_dispatch: # allows manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f3005146@ds.study.iitm.ac.in
        run: echo "Hello, world! This is my GitHub Action running!"
